#include <bits/stdc++.h>
using namespace std;

void permutations(vector<int> boxes, int ci, int ti)
{
    // write your code here

    cout << ci << " here" << ti;
}

int main()
{
    int nbox;
    cin >> nbox;
    int ritem;
    cin >> ritem;
    vector<int> num(nbox);
    permutations(num, nbox, ritem);

    return 0;
}
