#include <bits\stdc++.h>
using namespace std;

int main()
{
    vector<int> nums(5);
    int n = nums.size();
    for (int i = 0; i < n; i++)
    {
        cin >> nums[i];
    }
    int k;
    cin >> k;

    unordered_map<int, int> big;
    unordered_map<int, int> small;

    int str = 0;
    int cnt = 0;

    

    
    cout << "\ncount is " << cnt;

    // for (auto x : freq)

    // {
    //     cout << x.second;
    // }

    return 0;
}