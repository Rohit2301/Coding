#include <iostream>
#include <vector>
#include <numeric>
#include <math.h>
using namespace std;

int main()
{
    vector<int> nums1(6);
    for (int i = 0; i < nums1.size(); i++)
    {
        cin >> nums1[i];
    }
    vector<int> nums2(6);
    for (int i = 0; i < nums2.size(); i++)
    {
        cin >> nums2[i];
    }
    double res = 0;
      
    return 0;
}
